---
extends: existence
message: "Keep the footnote on one line or start it on the next line, indented by 4 spaces."
level: error
scope: raw
raw:
  - '(?m)^\[\^[^\]]+\]:(?:[ \t]*\S[^\n]*\n(?!\[\^[^\]]+\]:)[ \t]*\S|[ \t]*\n(?! {4}[^ \t]))'
