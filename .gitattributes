* annex.largefiles=nothing
/content/media/files/lldpd/* annex.largefiles=anything
/content/media/files/udpproxy/* annex.largefiles=anything
/content/media/videos/* annex.largefiles=mimetype=video/*

# There is also a hook configured:

# #!/bin/sh
# # automatically configured by git-annex
# git annex pre-commit .

# # git add stores annexed files unlocked, lock them back to symlinks
# git diff --cached --name-only --diff-filter=ACMRT -z |
# 	xargs -0 -r git annex find --unlocked --print0 -- |
# 	xargs -0 -r git annex lock --
